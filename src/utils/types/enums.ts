export enum requestStatus {
  SUCCESS = "success",
  PENDING = "pending",
  ERROR = "error",
  IDLE = "idle",
}

export enum roles {
  USER = "user",
  ADMIN = "admin",
}

export enum adminTools {
  CATEGORY = "category",
  SIZE = "size",
  COLOR = "color",
}
